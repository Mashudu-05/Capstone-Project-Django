.. vogue_maternity documentation master file, created by
   sphinx-quickstart on Mon May 12 21:05:27 2025.
   You can adapt this file completely to your liking, but it should at least
   contain the root `toctree` directive.

Welcome to your vogue_maternity documentation
=============================

Add your content using ``reStructuredText`` syntax. See the
`reStructuredText <https://www.sphinx-doc.org/en/master/usage/restructuredtext/index.html>`_
documentation for details.

.. Django Documentation Master file, created by
   sphinx-q

This is the main entry point for the documentation of your Django project.

Contents:

.. toctree::
   :maxdepth: 2
   :caption: Contents:

   modules

   installation
   usage
   api
   contributing
   github
   docker
   requirements

Installation
============

To install the Django project, follow these steps:

1. Clone the repository:

   .. code-block:: bash
      git clone https://github.com/yourusername/django-project.git

2. Install dependencies:

   .. code-block:: bash
      pip install -r requirements.txt

Usage
=====

Docker Hub
===========

You can pull the pre-built Docker image for this project from Docker Hub.

To run the Django app in a Docker container:

1. Build the Docker image:

   .. code-block:: bash
      docker build -t mashudu5/vogue_maternity .

2. Run the Docker container:

   .. code-block:: bash
      docker run -p 8000:8000 mashudu5/vogue_maternity

GitHub Repository
================

The code for this project is hosted on GitHub. You can access it here:

 `https://github.com/Mashudu-05/Capstone-Project-Django`

Requirements
============

This project requires certain dependencies that can be installed via the `requirements.txt` file. To install all dependencies, run:

.. code-block:: bash
   pip install -r requirements.txt

This will automatically



